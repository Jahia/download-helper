query GetDownloadHelperInfo {
    downloadHelper {
        info {
            isProcessingServer
            availableSpace
            downloadFolderPath
            isMailActivated
        }
    }
}
